<!DOCTYPE html>
<html>
    <head>
        @Html.Partial("_partials/_head")
        @Html.Partial("_partials/_contactHead")
        @RenderSection("head", required: false)
    </head>
    <body class="main">
        @Html.Partial("_partials/_header")
    <main id="front_page_container" role="main" style="display: flex; flex-wrap: wrap; width: 100%;">
            <section class="body-section">
                <div class="body-container">
                    @RenderBody()
                </div>
            </section>
        </main>
        <footer id="footer">
            @Html.Partial("_partials/_footer")
        </footer>
    </body>
</html>